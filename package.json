{
  "name": "qrcode01",
  "version": "1.0.0",
  "description": "Azure Functions app that generates QR code PNG images over HTTP.",
  "scripts": {
    "start": "func start",
    "deploy:package": "zip -r deploy.zip . -x '*.git*' -x 'local.settings.json' -x 'local.settings.example.json' -x 'deploy.zip'",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/functions": "^4.0.0",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "azure-functions-core-tools": "^4.13.0"
  },
  "main": "src/{index.js,functions/*.js}"
}
